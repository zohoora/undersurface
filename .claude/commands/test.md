Run tests for UnderSurface.

If $ARGUMENTS is provided, run only the matching test file:
```bash
npx vitest run $ARGUMENTS
```

If no arguments, run the full test suite:
```bash
npm run test
```

Report results. If tests fail, show the failure details and suggest fixes.
